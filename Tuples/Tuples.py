#Tuples are data structure as a container used to store values
# values in tuple are stored in parenthesis (<touple>).
# tuples are immutable once we create a touple we can't change it's values or items

coordinates = (3, 9)
print(coordinates[1])

#We can store tuples in a list but that can be changed

grades = [(250),(539), (820),(928)]
grades[2] = 39
print(grades)

